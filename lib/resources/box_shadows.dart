import 'package:flutter/material.dart';

class AppBoxShadows {
  static const BoxShadow home_page_start_training_button = BoxShadow(
    blurRadius: 5,
    spreadRadius: 1,
    color: Colors.grey,
    offset: Offset(0, 5),
  );
}
